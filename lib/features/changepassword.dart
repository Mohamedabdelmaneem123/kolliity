import 'package:flutter/material.dart';

class chandpassword extends StatelessWidget {
  const chandpassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
